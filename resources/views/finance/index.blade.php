@extends('layouts.app')
@section('content')
<div class="container-fluid py-4"><h1 class="h3 mb-4">Keuangan Cabang</h1>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif@if($errors->any())<div class="alert alert-danger"><ul class="mb-0">@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif<div class="row g-3"><div class="col-lg-8"><div class="card"><div class="card-body"><form class="row g-2"><div class="col-md-4"><label class="form-label">Cabang</label><select name="branch_id" class="form-select">@foreach($branches as $branch)<option value="{{ $branch->id }}" @selected($branch->id === $branchId)>{{ $branch->name }}</option>@endforeach</select></div><div class="col-md-3"><label class="form-label">Dari</label><input type="date" name="from" class="form-control" value="{{ $from }}"></div><div class="col-md-3"><label class="form-label">Sampai</label><input type="date" name="to" class="form-control" value="{{ $to }}"></div><div class="col-md-2 align-self-end"><button class="btn btn-primary w-100">Tampilkan</button></div></form></div></div><div class="row g-3 mt-1">@foreach([['Omzet POS',$report['sales_total'] ?? 0],['Kas masuk',$report['cash_income'] ?? 0],['Biaya operasional',$report['cash_expense'] ?? 0]] as [$label,$value])<div class="col-md-4"><div class="card"><div class="card-body"><div class="text-muted">{{ $label }}</div><div class="fs-4 fw-bold">Rp {{ number_format($value, 0, ',', '.') }}</div></div></div></div>@endforeach</div><div class="card mt-3"><div class="card-header">Pengeluaran periode</div><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Waktu</th><th>Kategori</th><th>Keterangan</th><th class="text-end">Nominal</th></tr></thead><tbody>@forelse($report['expenses'] ?? [] as $expense)<tr><td>{{ $expense->occurred_at->format('d/m/Y H:i') }}</td><td>{{ $expense->category }}</td><td>{{ $expense->description }}</td><td class="text-end">Rp {{ number_format($expense->amount, 0, ',', '.') }}</td></tr>@empty<tr><td colspan="4" class="text-center py-3">Belum ada pengeluaran.</td></tr>@endforelse</tbody></table></div></div></div><div class="col-lg-4"><div class="card mb-3"><div class="card-header">Buka shift</div><div class="card-body"><form method="post" action="{{ route('finance.shifts.open') }}">@csrf<input type="hidden" name="branch_id" value="{{ $branchId }}"><label class="form-label">Saldo awal</label><input name="opening_balance" type="number" min="0" step="0.01" class="form-control mb-2" required><button class="btn btn-success w-100">Buka shift</button></form></div></div><div class="card mb-3"><div class="card-header">Kas masuk/keluar</div><div class="card-body"><form method="post" action="{{ route('finance.transactions.store') }}">@csrf<input type="hidden" name="branch_id" value="{{ $branchId }}"><select name="type" class="form-select mb-2"><option value="expense">Kas keluar</option><option value="income">Kas masuk</option></select><input name="category" class="form-control mb-2" placeholder="Listrik, sewa, dll" required><input name="amount" type="number" min="0.01" step="0.01" class="form-control mb-2" placeholder="Nominal" required><input name="description" class="form-control mb-2" placeholder="Keterangan"><button class="btn btn-primary w-100">Catat transaksi</button></form></div></div>@foreach($openShifts as $shift)<div class="card mb-3"><div class="card-header">Shift terbuka · {{ $shift->branch->name }}</div><div class="card-body"><div>Saldo awal: Rp {{ number_format($shift->opening_balance, 0, ',', '.') }}</div><form method="post" action="{{ route('finance.shifts.close', $shift) }}" class="mt-2">@csrf<input name="closing_balance" type="number" min="0" step="0.01" class="form-control mb-2" placeholder="Saldo fisik akhir" required><input name="closing_note" class="form-control mb-2" placeholder="Catatan selisih"><button class="btn btn-warning w-100">Tutup shift</button></form></div></div>@endforeach</div></div></div>
@endsection
