@extends('layouts.app')
@section('content')
<div class="container-fluid py-4"><div class="d-flex justify-content-between align-items-center mb-3"><h1 class="h3">Produk dan Resep</h1>@if(auth()->user()->isCentralOwner())<a class="btn btn-primary" href="{{ route('products.create') }}">Tambah produk</a>@endif</div>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif@error('product')<div class="alert alert-danger">{{ $message }}</div>@enderror<div class="card"><div class="table-responsive"><table class="table mb-0"><thead><tr><th>SKU</th><th>Produk</th><th class="text-end">Harga jual</th><th>Resep</th><th>Status</th><th></th></tr></thead><tbody>@forelse($products as $product)<tr><td>{{ $product->sku }}</td><td>{{ $product->name }}</td><td class="text-end">Rp {{ number_format($product->selling_price, 0, ',', '.') }}</td><td>@forelse($product->recipes as $recipe){{ $recipe->ingredient->name }} ({{ $recipe->quantity }} {{ $recipe->ingredient->unit }})@if(!$loop->last), @endif @empty Belum ada resep @endforelse</td><td>{{ $product->is_active ? 'Aktif' : 'Nonaktif' }}</td><td>@if(auth()->user()->isCentralOwner())<a href="{{ route('products.edit', $product) }}">Edit</a>@endif</td></tr>@empty<tr><td colspan="6" class="text-center py-4">Belum ada produk.</td></tr>@endforelse</tbody></table></div><div class="card-footer">{{ $products->links() }}</div></div></div>
@endsection
