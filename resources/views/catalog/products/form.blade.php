@extends('layouts.app')
@section('content')
<div class="container py-4" style="max-width:1000px"><h1 class="h3 mb-4">{{ $product->exists ? 'Edit' : 'Tambah' }} Produk dan Resep</h1>@if($errors->any())<div class="alert alert-danger"><ul class="mb-0">@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif<form method="post" action="{{ $product->exists ? route('products.update', $product) : route('products.store') }}">@csrf@if($product->exists)@method('PUT')@endif<div class="card mb-3"><div class="card-body"><label class="form-label">Nama produk</label><input name="name" class="form-control mb-3" value="{{ old('name', $product->name) }}" required><div class="row"><div class="col-md-6"><label class="form-label">SKU</label><input name="sku" class="form-control" value="{{ old('sku', $product->sku) }}" required></div><div class="col-md-6"><label class="form-label">Harga jual</label><input name="selling_price" type="number" min="0" step="0.01" class="form-control" value="{{ old('selling_price', $product->selling_price ?? 0) }}" required></div></div><div class="form-check mt-3"><input class="form-check-input" type="checkbox" name="is_active" value="1" id="active" @checked(old('is_active', $product->exists ? $product->is_active : true))><label class="form-check-label" for="active">Aktif</label></div></div></div><div class="card"><div class="card-header d-flex justify-content-between"><span>Komposisi resep</span><button class="btn btn-sm btn-outline-primary" type="button" onclick="tambahResep()">Tambah bahan</button></div><div class="card-body" id="recipes">@foreach($product->recipes as $recipe)<div class="row g-2 mb-2 recipe-row"><div class="col-md-8"><select name="recipes[{{ $loop->index }}][ingredient_id]" class="form-select"><option value="">Pilih bahan</option>@foreach($ingredients as $ingredient)<option value="{{ $ingredient->id }}" @selected($ingredient->id === $recipe->ingredient_id)>{{ $ingredient->name }} ({{ $ingredient->unit }})</option>@endforeach</select></div><div class="col-md-3"><input name="recipes[{{ $loop->index }}][quantity]" type="number" min="0.001" step="0.001" class="form-control" value="{{ $recipe->quantity }}"></div><div class="col-md-1"><button class="btn btn-outline-danger" type="button" onclick="this.closest('.recipe-row').remove()">Hapus</button></div></div>@endforeach</div><div class="card-footer"><button class="btn btn-primary">Simpan</button> <a class="btn btn-link" href="{{ route('products.index') }}">Batal</a></div></div></form></div><template id="recipe-template"><div class="row g-2 mb-2 recipe-row"><div class="col-md-8"><select name="recipes[__INDEX__][ingredient_id]" class="form-select"><option value="">Pilih bahan</option>@foreach($ingredients as $ingredient)<option value="{{ $ingredient->id }}">{{ $ingredient->name }} ({{ $ingredient->unit }})</option>@endforeach</select></div><div class="col-md-3"><input name="recipes[__INDEX__][quantity]" type="number" min="0.001" step="0.001" class="form-control" placeholder="Jumlah"></div><div class="col-md-1"><button class="btn btn-outline-danger" type="button" onclick="this.closest('.recipe-row').remove()">Hapus</button></div></div></template><script>let i=0;function tambahResep(){document.getElementById('recipes').insertAdjacentHTML('beforeend',document.getElementById('recipe-template').innerHTML.replaceAll('__INDEX__',i++))}@foreach($product->recipes as $recipe) tambahResep(); @endforeach @if(!$product->exists) tambahResep(); @endif</script>
@endsection
