@extends('layouts.app')
@section('content')
<div class="container-fluid py-4"><div class="d-flex justify-content-between align-items-center mb-3"><h1 class="h3">Bahan Baku</h1>@if(auth()->user()->isCentralOwner())<a class="btn btn-primary" href="{{ route('ingredients.create') }}">Tambah bahan</a>@endif</div>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif@error('ingredient')<div class="alert alert-danger">{{ $message }}</div>@enderror<div class="card"><div class="table-responsive"><table class="table mb-0"><thead><tr><th>SKU</th><th>Nama</th><th>Satuan</th><th class="text-end">Harga</th><th class="text-end">ROP</th><th>Status</th><th></th></tr></thead><tbody>@forelse($ingredients as $ingredient)<tr><td>{{ $ingredient->sku }}</td><td>{{ $ingredient->name }}</td><td>{{ $ingredient->unit }}</td><td class="text-end">Rp {{ number_format($ingredient->cost, 0, ',', '.') }}</td><td class="text-end">{{ $ingredient->reorder_point }}</td><td>{{ $ingredient->is_active ? 'Aktif' : 'Nonaktif' }}</td><td>@if(auth()->user()->isCentralOwner())<a href="{{ route('ingredients.edit', $ingredient) }}">Edit</a>@endif</td></tr>@empty<tr><td colspan="7" class="text-center py-4">Belum ada bahan.</td></tr>@endforelse</tbody></table></div><div class="card-footer">{{ $ingredients->links() }}</div></div></div>
@endsection
