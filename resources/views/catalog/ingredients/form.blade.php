@extends('layouts.app')
@section('content')
<div class="container py-4" style="max-width:800px"><h1 class="h3 mb-4">{{ $ingredient->exists ? 'Edit' : 'Tambah' }} Bahan Baku</h1>@if($errors->any())<div class="alert alert-danger"><ul class="mb-0">@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif<form method="post" action="{{ $ingredient->exists ? route('ingredients.update', $ingredient) : route('ingredients.store') }}">@csrf@if($ingredient->exists)@method('PUT')@endif<div class="card"><div class="card-body"><label class="form-label">Nama</label><input name="name" class="form-control mb-3" value="{{ old('name', $ingredient->name) }}" required><label class="form-label">SKU</label><input name="sku" class="form-control mb-3" value="{{ old('sku', $ingredient->sku) }}" required><div class="row"><div class="col-md-4"><label class="form-label">Satuan</label><input name="unit" class="form-control" placeholder="kg, gram, pcs" value="{{ old('unit', $ingredient->unit) }}" required></div><div class="col-md-4"><label class="form-label">Harga pokok</label><input name="cost" type="number" min="0" step="0.01" class="form-control" value="{{ old('cost', $ingredient->cost ?? 0) }}" required></div><div class="col-md-4"><label class="form-label">Reorder point</label><input name="reorder_point" type="number" min="0" step="0.001" class="form-control" value="{{ old('reorder_point', $ingredient->reorder_point ?? 0) }}" required></div></div><div class="form-check mt-3"><input class="form-check-input" type="checkbox" name="is_active" value="1" id="active" @checked(old('is_active', $ingredient->exists ? $ingredient->is_active : true))><label class="form-check-label" for="active">Aktif</label></div></div><div class="card-footer"><button class="btn btn-primary">Simpan</button> <a class="btn btn-link" href="{{ route('ingredients.index') }}">Batal</a></div></div></form></div>
@endsection
