@extends('layouts.app')
@section('content')
<div class="container-fluid py-4"><div class="d-flex justify-content-between align-items-center mb-3"><h1 class="h3">Purchase Order</h1>@if(auth()->user()->hasRole('owner-mitra'))<a class="btn btn-primary" href="{{ route('purchase-orders.create') }}">Buat PO</a>@endif</div>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<div class="card"><div class="table-responsive"><table class="table mb-0"><thead><tr><th>ID</th><th>Cabang</th><th>Pembuat</th><th>Status</th><th>Dibuat</th><th></th></tr></thead><tbody>@forelse($orders as $order)<tr><td>#{{ $order->id }}</td><td>{{ $order->branch->name }}</td><td>{{ $order->creator->name }}</td><td><span class="badge text-bg-secondary">{{ strtoupper($order->status) }}</span></td><td>{{ $order->created_at->format('d/m/Y H:i') }}</td><td><a href="{{ route('purchase-orders.show', $order) }}">Detail</a></td></tr>@empty<tr><td colspan="6" class="text-center py-4">Belum ada PO.</td></tr>@endforelse</tbody></table></div><div class="card-footer">{{ $orders->links() }}</div></div></div>
@endsection
